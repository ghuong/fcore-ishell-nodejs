
const externalState = 0;

const pureExpression = (a) => {
  externalState = a;
}

const puretest = require("puretest");

module.exports = {
  pureExpression,
  _puretests: puretest(pureExpression, 1)
}